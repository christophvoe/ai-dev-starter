Before writing any code, create an implementation plan for: $ARGUMENTS

Follow the Plan -> Implement -> Verify -> Adjust pattern.

Your plan must include:

1. **Goal**: What this achieves (one sentence)
2. **Approach**: Files to create/modify, with brief description of each change
3. **Edge Cases**: What could go wrong?
   - Empty/None/malformed input
   - Network timeouts, 403/429 responses
   - File conflicts, missing directories
   - Windows vs Unix differences (paths, encoding)
4. **Dependencies**: New packages needed? Existing code to reuse?
5. **Testing Plan**: What tests to write, what to mock
6. **Tradeoffs**: Alternative approaches and why this one is better
7. **Scope**: Small (1-2 files) / Medium (3-5) / Large (6+)

Do NOT write implementation code. Output the plan only. I will review before proceeding.
